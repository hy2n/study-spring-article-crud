spring:
  datasource:
    url: jdbc:mysql://localhost:3306/study_spring_crud_table
    username: root
    password: mysqlpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  main:
    banner-mode: "off"

server:
  port: 8080

logging:
  level:
    root: INFO
